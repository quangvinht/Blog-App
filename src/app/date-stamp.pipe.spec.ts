import { DateStampPipe } from './date-stamp.pipe';

describe('DateStampPipe', () => {
  it('create an instance', () => {
    const pipe = new DateStampPipe();
    expect(pipe).toBeTruthy();
  });
});
